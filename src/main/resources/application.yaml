spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/migration?currentSchema=sb_liquibase'
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true